# Swift 키워드 영어

> 예상 학습 시간: 30~45분

## 1. 학습 목표

* 코드, 오류, 문서에서 반복되는 영어를 단어 단위로 분해한다.
* 일반 의미보다 개발 문맥에서의 역할을 먼저 떠올린다.
* Swift/SwiftUI 예제, 검색어, 면접 답변으로 연결한다.

## 2. 핵심 단어

| 영어 | 발음 참고 | 개발 문맥 의미 | 일반 의미 |
|---|---|---|---|
| let | let | Swift/iOS 문맥에서 let가 가리키는 역할을 파악한다 | 일반 영어의 기본 의미 |
| var | var | Swift/iOS 문맥에서 var가 가리키는 역할을 파악한다 | 일반 영어의 기본 의미 |
| func | fun | Swift/iOS 문맥에서 func가 가리키는 역할을 파악한다 | 일반 영어의 기본 의미 |
| struct | str | Swift/iOS 문맥에서 struct가 가리키는 역할을 파악한다 | 일반 영어의 기본 의미 |
| class | cla | Swift/iOS 문맥에서 class가 가리키는 역할을 파악한다 | 일반 영어의 기본 의미 |
| enum | enu | Swift/iOS 문맥에서 enum가 가리키는 역할을 파악한다 | 일반 영어의 기본 의미 |
| protocol | pro | Swift/iOS 문맥에서 protocol가 가리키는 역할을 파악한다 | 일반 영어의 기본 의미 |
| extension | ext | Swift/iOS 문맥에서 extension가 가리키는 역할을 파악한다 | 일반 영어의 기본 의미 |
| guard | gua | Swift/iOS 문맥에서 guard가 가리키는 역할을 파악한다 | 일반 영어의 기본 의미 |
| if | if | Swift/iOS 문맥에서 if가 가리키는 역할을 파악한다 | 일반 영어의 기본 의미 |
| else | els | Swift/iOS 문맥에서 else가 가리키는 역할을 파악한다 | 일반 영어의 기본 의미 |
| switch | swi | Swift/iOS 문맥에서 switch가 가리키는 역할을 파악한다 | 일반 영어의 기본 의미 |

## 3. 단어 구성 분석

```text
configuration = configure + -ation = 설정하는 행위 또는 설정 정보
invalid = in- + valid = 유효하지 않은 상태
asynchronous = a- + synchronous = 동시에 맞춰 기다리지 않는 실행 방식
```

## 4. 실제 코드에서 보기

```swift
struct UserProfileView: View {
    @State private var isLoading = false

    var body: some View {
        VStack {
            Text(isLoading ? "Loading..." : "Profile")
            Button("Refresh") {
                Task { await fetchUserProfile() }
            }
        }
    }

    func fetchUserProfile() async {
        isLoading = true
        defer { isLoading = false }
    }
}
```

* `fetchUserProfile`: fetch + User + Profile, 사용자 프로필을 가져오는 함수
* `isLoading`: 현재 로딩 상태인지 나타내는 Boolean 이름
* `defer`: 함수가 끝날 때 실행할 정리 작업

## 5. Xcode 또는 문서에서 보기

* `Build failed`: 빌드가 실패했다.
* `This property is available in iOS 17.0 and later`: 이 속성은 iOS 17 이상에서 사용할 수 있다.
* `Cannot find 'user' in scope`: 현재 범위에서 `user`를 찾을 수 없다.

## 6. 문장 끊어 읽기

```text
Cannot convert / value of type 'String' / to expected argument type 'Int'
변환할 수 없다 / String 타입의 값을 / 예상되는 Int 인자 타입으로
```

## 7. 자연스러운 한국어 해석

* 직역: String 타입의 값을 예상되는 Int 인자 타입으로 변환할 수 없다.
* 개발자식 해석: Int가 필요한 자리에 String을 넣었다.

## 8. 비슷한 단어 비교

* create: 새로 만든다. `createAccount()`
* generate: 규칙이나 데이터로 만들어 낸다. `generateToken()`
* initialize: 초기값을 넣어 준비한다. `initializeStore()`
* configure: 설정값을 적용한다. `configureCell()`
* update: 이미 있는 값을 바꾼다. `updateProfile()`

## 9. 자주 하는 오해

* `argument`와 `parameter`를 모두 “파라미터”로만 외우면 오류 메시지에서 헷갈린다. 호출하는 쪽 값은 argument, 함수 선언의 자리는 parameter다.
* `state`는 단순한 “상태”가 아니라 화면을 다시 그리게 만드는 데이터일 수 있다.

## 10. 직접 해석 문제

1. `Missing argument for parameter 'title' in call`
2. `The request failed because the token is invalid.`
3. `Update the selected item when the user taps a row.`

## 11. 검색어 만들기

* `SwiftUI state not updating after async request`
* `Swift cannot convert String to Int expected argument type`
* `Xcode build failed provisioning profile does not match`

## 12. AI에게 요청하기

```text
Explain the following Swift error in simple Korean.
Identify the expected type, the actual type, and the smallest fix.
```

## 13. 면접에서 사용하기

* `I validate user input before sending a request.`
* `I handle errors by showing a user-friendly message.`
* `I use state to update the SwiftUI view automatically.`

## 14. 오늘의 복습 카드

### `let`

* 일반 의미: `let`라는 영어 단서에서 역할을 추론한다.
* Swift 의미: 선언, 흐름 제어, 오류 처리, 비동기, 접근 제어 중 하나로 코드를 읽게 해 준다.
* 예제:

```swift
// let 키워드가 실제 코드에서 의미를 만드는 위치를 확인한다.
```

* 개발자식 해석: 키워드를 번역하지 말고 “이 줄이 어떤 규칙을 만드는가”를 먼저 본다.

### `var`

* 일반 의미: `var`라는 영어 단서에서 역할을 추론한다.
* Swift 의미: 선언, 흐름 제어, 오류 처리, 비동기, 접근 제어 중 하나로 코드를 읽게 해 준다.
* 예제:

```swift
// var 키워드가 실제 코드에서 의미를 만드는 위치를 확인한다.
```

* 개발자식 해석: 키워드를 번역하지 말고 “이 줄이 어떤 규칙을 만드는가”를 먼저 본다.

### `func`

* 일반 의미: `func`라는 영어 단서에서 역할을 추론한다.
* Swift 의미: 선언, 흐름 제어, 오류 처리, 비동기, 접근 제어 중 하나로 코드를 읽게 해 준다.
* 예제:

```swift
// func 키워드가 실제 코드에서 의미를 만드는 위치를 확인한다.
```

* 개발자식 해석: 키워드를 번역하지 말고 “이 줄이 어떤 규칙을 만드는가”를 먼저 본다.

### `struct`

* 일반 의미: `struct`라는 영어 단서에서 역할을 추론한다.
* Swift 의미: 선언, 흐름 제어, 오류 처리, 비동기, 접근 제어 중 하나로 코드를 읽게 해 준다.
* 예제:

```swift
// struct 키워드가 실제 코드에서 의미를 만드는 위치를 확인한다.
```

* 개발자식 해석: 키워드를 번역하지 말고 “이 줄이 어떤 규칙을 만드는가”를 먼저 본다.

### `class`

* 일반 의미: `class`라는 영어 단서에서 역할을 추론한다.
* Swift 의미: 선언, 흐름 제어, 오류 처리, 비동기, 접근 제어 중 하나로 코드를 읽게 해 준다.
* 예제:

```swift
// class 키워드가 실제 코드에서 의미를 만드는 위치를 확인한다.
```

* 개발자식 해석: 키워드를 번역하지 말고 “이 줄이 어떤 규칙을 만드는가”를 먼저 본다.

### `enum`

* 일반 의미: `enum`라는 영어 단서에서 역할을 추론한다.
* Swift 의미: 선언, 흐름 제어, 오류 처리, 비동기, 접근 제어 중 하나로 코드를 읽게 해 준다.
* 예제:

```swift
// enum 키워드가 실제 코드에서 의미를 만드는 위치를 확인한다.
```

* 개발자식 해석: 키워드를 번역하지 말고 “이 줄이 어떤 규칙을 만드는가”를 먼저 본다.

### `protocol`

* 일반 의미: `protocol`라는 영어 단서에서 역할을 추론한다.
* Swift 의미: 선언, 흐름 제어, 오류 처리, 비동기, 접근 제어 중 하나로 코드를 읽게 해 준다.
* 예제:

```swift
// protocol 키워드가 실제 코드에서 의미를 만드는 위치를 확인한다.
```

* 개발자식 해석: 키워드를 번역하지 말고 “이 줄이 어떤 규칙을 만드는가”를 먼저 본다.

### `extension`

* 일반 의미: `extension`라는 영어 단서에서 역할을 추론한다.
* Swift 의미: 선언, 흐름 제어, 오류 처리, 비동기, 접근 제어 중 하나로 코드를 읽게 해 준다.
* 예제:

```swift
// extension 키워드가 실제 코드에서 의미를 만드는 위치를 확인한다.
```

* 개발자식 해석: 키워드를 번역하지 말고 “이 줄이 어떤 규칙을 만드는가”를 먼저 본다.

### `guard`

* 일반 의미: `guard`라는 영어 단서에서 역할을 추론한다.
* Swift 의미: 선언, 흐름 제어, 오류 처리, 비동기, 접근 제어 중 하나로 코드를 읽게 해 준다.
* 예제:

```swift
// guard 키워드가 실제 코드에서 의미를 만드는 위치를 확인한다.
```

* 개발자식 해석: 키워드를 번역하지 말고 “이 줄이 어떤 규칙을 만드는가”를 먼저 본다.

### `if`

* 일반 의미: `if`라는 영어 단서에서 역할을 추론한다.
* Swift 의미: 선언, 흐름 제어, 오류 처리, 비동기, 접근 제어 중 하나로 코드를 읽게 해 준다.
* 예제:

```swift
// if 키워드가 실제 코드에서 의미를 만드는 위치를 확인한다.
```

* 개발자식 해석: 키워드를 번역하지 말고 “이 줄이 어떤 규칙을 만드는가”를 먼저 본다.

### `else`

* 일반 의미: `else`라는 영어 단서에서 역할을 추론한다.
* Swift 의미: 선언, 흐름 제어, 오류 처리, 비동기, 접근 제어 중 하나로 코드를 읽게 해 준다.
* 예제:

```swift
// else 키워드가 실제 코드에서 의미를 만드는 위치를 확인한다.
```

* 개발자식 해석: 키워드를 번역하지 말고 “이 줄이 어떤 규칙을 만드는가”를 먼저 본다.

### `switch`

* 일반 의미: `switch`라는 영어 단서에서 역할을 추론한다.
* Swift 의미: 선언, 흐름 제어, 오류 처리, 비동기, 접근 제어 중 하나로 코드를 읽게 해 준다.
* 예제:

```swift
// switch 키워드가 실제 코드에서 의미를 만드는 위치를 확인한다.
```

* 개발자식 해석: 키워드를 번역하지 말고 “이 줄이 어떤 규칙을 만드는가”를 먼저 본다.

### `case`

* 일반 의미: `case`라는 영어 단서에서 역할을 추론한다.
* Swift 의미: 선언, 흐름 제어, 오류 처리, 비동기, 접근 제어 중 하나로 코드를 읽게 해 준다.
* 예제:

```swift
// case 키워드가 실제 코드에서 의미를 만드는 위치를 확인한다.
```

* 개발자식 해석: 키워드를 번역하지 말고 “이 줄이 어떤 규칙을 만드는가”를 먼저 본다.

### `for`

* 일반 의미: `for`라는 영어 단서에서 역할을 추론한다.
* Swift 의미: 선언, 흐름 제어, 오류 처리, 비동기, 접근 제어 중 하나로 코드를 읽게 해 준다.
* 예제:

```swift
// for 키워드가 실제 코드에서 의미를 만드는 위치를 확인한다.
```

* 개발자식 해석: 키워드를 번역하지 말고 “이 줄이 어떤 규칙을 만드는가”를 먼저 본다.

### `while`

* 일반 의미: `while`라는 영어 단서에서 역할을 추론한다.
* Swift 의미: 선언, 흐름 제어, 오류 처리, 비동기, 접근 제어 중 하나로 코드를 읽게 해 준다.
* 예제:

```swift
// while 키워드가 실제 코드에서 의미를 만드는 위치를 확인한다.
```

* 개발자식 해석: 키워드를 번역하지 말고 “이 줄이 어떤 규칙을 만드는가”를 먼저 본다.

### `return`

* 일반 의미: `return`라는 영어 단서에서 역할을 추론한다.
* Swift 의미: 선언, 흐름 제어, 오류 처리, 비동기, 접근 제어 중 하나로 코드를 읽게 해 준다.
* 예제:

```swift
// return 키워드가 실제 코드에서 의미를 만드는 위치를 확인한다.
```

* 개발자식 해석: 키워드를 번역하지 말고 “이 줄이 어떤 규칙을 만드는가”를 먼저 본다.

### `throw`

* 일반 의미: `throw`라는 영어 단서에서 역할을 추론한다.
* Swift 의미: 선언, 흐름 제어, 오류 처리, 비동기, 접근 제어 중 하나로 코드를 읽게 해 준다.
* 예제:

```swift
// throw 키워드가 실제 코드에서 의미를 만드는 위치를 확인한다.
```

* 개발자식 해석: 키워드를 번역하지 말고 “이 줄이 어떤 규칙을 만드는가”를 먼저 본다.

### `throws`

* 일반 의미: `throws`라는 영어 단서에서 역할을 추론한다.
* Swift 의미: 선언, 흐름 제어, 오류 처리, 비동기, 접근 제어 중 하나로 코드를 읽게 해 준다.
* 예제:

```swift
// throws 키워드가 실제 코드에서 의미를 만드는 위치를 확인한다.
```

* 개발자식 해석: 키워드를 번역하지 말고 “이 줄이 어떤 규칙을 만드는가”를 먼저 본다.

### `try`

* 일반 의미: `try`라는 영어 단서에서 역할을 추론한다.
* Swift 의미: 선언, 흐름 제어, 오류 처리, 비동기, 접근 제어 중 하나로 코드를 읽게 해 준다.
* 예제:

```swift
// try 키워드가 실제 코드에서 의미를 만드는 위치를 확인한다.
```

* 개발자식 해석: 키워드를 번역하지 말고 “이 줄이 어떤 규칙을 만드는가”를 먼저 본다.

### `catch`

* 일반 의미: `catch`라는 영어 단서에서 역할을 추론한다.
* Swift 의미: 선언, 흐름 제어, 오류 처리, 비동기, 접근 제어 중 하나로 코드를 읽게 해 준다.
* 예제:

```swift
// catch 키워드가 실제 코드에서 의미를 만드는 위치를 확인한다.
```

* 개발자식 해석: 키워드를 번역하지 말고 “이 줄이 어떤 규칙을 만드는가”를 먼저 본다.

### `async`

* 일반 의미: `async`라는 영어 단서에서 역할을 추론한다.
* Swift 의미: 선언, 흐름 제어, 오류 처리, 비동기, 접근 제어 중 하나로 코드를 읽게 해 준다.
* 예제:

```swift
// async 키워드가 실제 코드에서 의미를 만드는 위치를 확인한다.
```

* 개발자식 해석: 키워드를 번역하지 말고 “이 줄이 어떤 규칙을 만드는가”를 먼저 본다.

### `await`

* 일반 의미: `await`라는 영어 단서에서 역할을 추론한다.
* Swift 의미: 선언, 흐름 제어, 오류 처리, 비동기, 접근 제어 중 하나로 코드를 읽게 해 준다.
* 예제:

```swift
// await 키워드가 실제 코드에서 의미를 만드는 위치를 확인한다.
```

* 개발자식 해석: 키워드를 번역하지 말고 “이 줄이 어떤 규칙을 만드는가”를 먼저 본다.

### `actor`

* 일반 의미: `actor`라는 영어 단서에서 역할을 추론한다.
* Swift 의미: 선언, 흐름 제어, 오류 처리, 비동기, 접근 제어 중 하나로 코드를 읽게 해 준다.
* 예제:

```swift
// actor 키워드가 실제 코드에서 의미를 만드는 위치를 확인한다.
```

* 개발자식 해석: 키워드를 번역하지 말고 “이 줄이 어떤 규칙을 만드는가”를 먼저 본다.

### `some`

* 일반 의미: `some`라는 영어 단서에서 역할을 추론한다.
* Swift 의미: 선언, 흐름 제어, 오류 처리, 비동기, 접근 제어 중 하나로 코드를 읽게 해 준다.
* 예제:

```swift
// some 키워드가 실제 코드에서 의미를 만드는 위치를 확인한다.
```

* 개발자식 해석: 키워드를 번역하지 말고 “이 줄이 어떤 규칙을 만드는가”를 먼저 본다.

### `any`

* 일반 의미: `any`라는 영어 단서에서 역할을 추론한다.
* Swift 의미: 선언, 흐름 제어, 오류 처리, 비동기, 접근 제어 중 하나로 코드를 읽게 해 준다.
* 예제:

```swift
// any 키워드가 실제 코드에서 의미를 만드는 위치를 확인한다.
```

* 개발자식 해석: 키워드를 번역하지 말고 “이 줄이 어떤 규칙을 만드는가”를 먼저 본다.

### `inout`

* 일반 의미: `inout`라는 영어 단서에서 역할을 추론한다.
* Swift 의미: 선언, 흐름 제어, 오류 처리, 비동기, 접근 제어 중 하나로 코드를 읽게 해 준다.
* 예제:

```swift
// inout 키워드가 실제 코드에서 의미를 만드는 위치를 확인한다.
```

* 개발자식 해석: 키워드를 번역하지 말고 “이 줄이 어떤 규칙을 만드는가”를 먼저 본다.

### `where`

* 일반 의미: `where`라는 영어 단서에서 역할을 추론한다.
* Swift 의미: 선언, 흐름 제어, 오류 처리, 비동기, 접근 제어 중 하나로 코드를 읽게 해 준다.
* 예제:

```swift
// where 키워드가 실제 코드에서 의미를 만드는 위치를 확인한다.
```

* 개발자식 해석: 키워드를 번역하지 말고 “이 줄이 어떤 규칙을 만드는가”를 먼저 본다.

### `associatedtype`

* 일반 의미: `associatedtype`라는 영어 단서에서 역할을 추론한다.
* Swift 의미: 선언, 흐름 제어, 오류 처리, 비동기, 접근 제어 중 하나로 코드를 읽게 해 준다.
* 예제:

```swift
// associatedtype 키워드가 실제 코드에서 의미를 만드는 위치를 확인한다.
```

* 개발자식 해석: 키워드를 번역하지 말고 “이 줄이 어떤 규칙을 만드는가”를 먼저 본다.

### `weak`

* 일반 의미: `weak`라는 영어 단서에서 역할을 추론한다.
* Swift 의미: 선언, 흐름 제어, 오류 처리, 비동기, 접근 제어 중 하나로 코드를 읽게 해 준다.
* 예제:

```swift
// weak 키워드가 실제 코드에서 의미를 만드는 위치를 확인한다.
```

* 개발자식 해석: 키워드를 번역하지 말고 “이 줄이 어떤 규칙을 만드는가”를 먼저 본다.

### `unowned`

* 일반 의미: `unowned`라는 영어 단서에서 역할을 추론한다.
* Swift 의미: 선언, 흐름 제어, 오류 처리, 비동기, 접근 제어 중 하나로 코드를 읽게 해 준다.
* 예제:

```swift
// unowned 키워드가 실제 코드에서 의미를 만드는 위치를 확인한다.
```

* 개발자식 해석: 키워드를 번역하지 말고 “이 줄이 어떤 규칙을 만드는가”를 먼저 본다.

### `final`

* 일반 의미: `final`라는 영어 단서에서 역할을 추론한다.
* Swift 의미: 선언, 흐름 제어, 오류 처리, 비동기, 접근 제어 중 하나로 코드를 읽게 해 준다.
* 예제:

```swift
// final 키워드가 실제 코드에서 의미를 만드는 위치를 확인한다.
```

* 개발자식 해석: 키워드를 번역하지 말고 “이 줄이 어떤 규칙을 만드는가”를 먼저 본다.

### `override`

* 일반 의미: `override`라는 영어 단서에서 역할을 추론한다.
* Swift 의미: 선언, 흐름 제어, 오류 처리, 비동기, 접근 제어 중 하나로 코드를 읽게 해 준다.
* 예제:

```swift
// override 키워드가 실제 코드에서 의미를 만드는 위치를 확인한다.
```

* 개발자식 해석: 키워드를 번역하지 말고 “이 줄이 어떤 규칙을 만드는가”를 먼저 본다.

### `mutating`

* 일반 의미: `mutating`라는 영어 단서에서 역할을 추론한다.
* Swift 의미: 선언, 흐름 제어, 오류 처리, 비동기, 접근 제어 중 하나로 코드를 읽게 해 준다.
* 예제:

```swift
// mutating 키워드가 실제 코드에서 의미를 만드는 위치를 확인한다.
```

* 개발자식 해석: 키워드를 번역하지 말고 “이 줄이 어떤 규칙을 만드는가”를 먼저 본다.

### `private`

* 일반 의미: `private`라는 영어 단서에서 역할을 추론한다.
* Swift 의미: 선언, 흐름 제어, 오류 처리, 비동기, 접근 제어 중 하나로 코드를 읽게 해 준다.
* 예제:

```swift
// private 키워드가 실제 코드에서 의미를 만드는 위치를 확인한다.
```

* 개발자식 해석: 키워드를 번역하지 말고 “이 줄이 어떤 규칙을 만드는가”를 먼저 본다.

### `internal`

* 일반 의미: `internal`라는 영어 단서에서 역할을 추론한다.
* Swift 의미: 선언, 흐름 제어, 오류 처리, 비동기, 접근 제어 중 하나로 코드를 읽게 해 준다.
* 예제:

```swift
// internal 키워드가 실제 코드에서 의미를 만드는 위치를 확인한다.
```

* 개발자식 해석: 키워드를 번역하지 말고 “이 줄이 어떤 규칙을 만드는가”를 먼저 본다.

### `public`

* 일반 의미: `public`라는 영어 단서에서 역할을 추론한다.
* Swift 의미: 선언, 흐름 제어, 오류 처리, 비동기, 접근 제어 중 하나로 코드를 읽게 해 준다.
* 예제:

```swift
// public 키워드가 실제 코드에서 의미를 만드는 위치를 확인한다.
```

* 개발자식 해석: 키워드를 번역하지 말고 “이 줄이 어떤 규칙을 만드는가”를 먼저 본다.

### `open`

* 일반 의미: `open`라는 영어 단서에서 역할을 추론한다.
* Swift 의미: 선언, 흐름 제어, 오류 처리, 비동기, 접근 제어 중 하나로 코드를 읽게 해 준다.
* 예제:

```swift
// open 키워드가 실제 코드에서 의미를 만드는 위치를 확인한다.
```

* 개발자식 해석: 키워드를 번역하지 말고 “이 줄이 어떤 규칙을 만드는가”를 먼저 본다.

### `static`

* 일반 의미: `static`라는 영어 단서에서 역할을 추론한다.
* Swift 의미: 선언, 흐름 제어, 오류 처리, 비동기, 접근 제어 중 하나로 코드를 읽게 해 준다.
* 예제:

```swift
// static 키워드가 실제 코드에서 의미를 만드는 위치를 확인한다.
```

* 개발자식 해석: 키워드를 번역하지 말고 “이 줄이 어떤 규칙을 만드는가”를 먼저 본다.

### `lazy`

* 일반 의미: `lazy`라는 영어 단서에서 역할을 추론한다.
* Swift 의미: 선언, 흐름 제어, 오류 처리, 비동기, 접근 제어 중 하나로 코드를 읽게 해 준다.
* 예제:

```swift
// lazy 키워드가 실제 코드에서 의미를 만드는 위치를 확인한다.
```

* 개발자식 해석: 키워드를 번역하지 말고 “이 줄이 어떤 규칙을 만드는가”를 먼저 본다.

### `defer`

* 일반 의미: `defer`라는 영어 단서에서 역할을 추론한다.
* Swift 의미: 선언, 흐름 제어, 오류 처리, 비동기, 접근 제어 중 하나로 코드를 읽게 해 준다.
* 예제:

```swift
// defer 키워드가 실제 코드에서 의미를 만드는 위치를 확인한다.
```

* 개발자식 해석: 키워드를 번역하지 말고 “이 줄이 어떤 규칙을 만드는가”를 먼저 본다.


## 15. 완료 체크

* [ ] 단어를 보고 의미를 떠올릴 수 있다.
* [ ] 코드 이름을 분해할 수 있다.
* [ ] 오류 메시지 핵심을 찾을 수 있다.
* [ ] 영어 검색어를 만들 수 있다.

## Swift 키워드 실제 예제

```swift
struct User: Identifiable {
    let id: UUID
    var name: String
}

final class UserStore {
    private var cachedUsers: [User] = []

    func loadUsers() async throws -> [User] {
        guard !cachedUsers.isEmpty else {
            throw UserError.empty
        }
        return cachedUsers
    }
}
```

* `struct`: 값 타입 모델을 선언한다.
* `final class`: 상속하지 않을 참조 타입을 선언한다.
* `private`: 이 타입 내부에서만 접근한다.
* `async throws`: 기다려야 하고 실패할 수 있다.

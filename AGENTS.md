# 저장소 공통 에이전트 안내 (Cursor / Codex 등 도구 공통)

이 파일은 [AGENTS.md](https://developers.openai.com/codex/guides/agents-md) 표준을
따르는 **도구 무관(cross-tool) 지침서**입니다. Codex는 세션 시작 시 이 파일을
자동으로 읽어 컨텍스트에 포함하고, Cursor도 저장소 루트의 `AGENTS.md`를 동일한
방식으로 읽습니다. 즉 아래 규칙은 어떤 도구로 이 저장소를 열든 동일하게 적용됩니다.

## 이 저장소에 있는 것

- `app_dev_org/` — crewAI 기반 "앱 개발 조직" (PM → 아키텍트 → 개발자 → QA
  순차 협업) + `git worktree` 기반 프로젝트 브랜치 격리 시스템.
  자세한 내용: `app_dev_org/README.md`
- `agent_dashboard/` — **Cursor Cloud Agent 전용** 현황 웹 대시보드
  (Cursor 공개 API만 사용). Codex 등 다른 도구의 세션은 여기 나타나지 않으며,
  이 파일 자체와는 무관합니다.

## 규칙: 새 앱/프로젝트 개발을 요청받았을 때 (중요, 반드시 준수)

사용자가 "새 프로젝트를 만들어줘", "○○ 앱 만들어줘" 같이 **처음부터 새로운
앱/프로젝트를 개발**해 달라고 요청하면, **현재 브랜치(조직 브랜치)에서 직접
파일을 만들지 말고** 아래 순서를 따르세요. 이 규칙은 Cursor, Codex 등
어떤 도구를 쓰고 있든 동일합니다 — 로직이 순수 Python + git 명령으로만
구현되어 있어 특정 도구의 내부 API에 의존하지 않기 때문입니다.

1. **가능하면** 이미 구현된 CLI를 그대로 사용하세요.

   ```bash
   cd app_dev_org
   pip install -r requirements.txt   # 최초 1회
   cp .env.example .env              # 최초 1회, LLM API 키(OPENAI_API_KEY 등) 입력
                                     # .env 의 ORG_BASE_BRANCH=main 설정 유지 권장
   python main.py new "<프로젝트 이름>" "<사용자 요구사항 원문>"
   ```

   CLI에는 안전장치가 내장되어 있습니다: API 키가 없거나, 프로젝트 브랜치
   위에서 또 분기하려 하거나, 같은 이름의 프로젝트가 이미 있으면 브랜치를
   만들기 전에 멈추고 해결 방법을 안내합니다. 현황 확인은
   `python main.py status` 를 사용하세요.

   이 명령이 자동으로 다음을 수행합니다.
   - 현재(조직) 브랜치에서 `project/<프로젝트 이름>` 브랜치를 분기
   - `.projects/<프로젝트 이름>/` 폴더(gitignore 처리됨)에 별도 체크아웃
   - crewAI 조직(PM→아키텍트→개발자→QA)이 그 폴더 안에만 산출물 생성
   - 산출물을 `project/<프로젝트 이름>` 브랜치에 커밋 (조직 브랜치는 무변경)

2. **LLM API 키가 없는 경우(기본 권장 모드)** — **"B 모드: 에이전트 단독
   조직 모드"**로 진행하세요. 이 모드에서는 지금 이 문서를 읽고 있는
   에이전트(Codex/Cursor 등)가 crewAI를 대신해 조직의 4가지 역할을 직접
   순서대로 수행합니다. LLM API 키가 전혀 필요 없습니다.

   **(1) 격리 작업공간 + 산출물 템플릿 생성** (API 키 불필요, 안전 가드 내장):

   ```bash
   cd app_dev_org
   python main.py prepare "<프로젝트 이름>" "<사용자 요구사항 원문>"
   ```

   이 명령은 격리된 `project/<이름>` 브랜치·폴더를 만들고, 그 안에
   `PROJECT_BRIEF.md`(요구사항+체크리스트), `docs/01~04` 템플릿,
   `logs/handoff.md`를 미리 깔아줍니다. `new`와 동일한 안전 가드(프로젝트
   브랜치 위 분기 방지, 중복 이름 차단)가 적용됩니다.

   (python 실행이 불가능한 환경이라면 예외적으로 수동 git 절차를 쓰세요:
   `git worktree add -b project/<이름> .projects/<이름> main`)

   **(2) 4가지 역할을 순서대로 수행** — 격리 폴더 안의 템플릿을 채우세요.
   각 템플릿 상단에 역할별 지시가 적혀 있습니다. 반드시 이전 단계 산출물을
   먼저 읽고 다음 단계를 진행하고, 각 단계를 마칠 때마다 `logs/handoff.md`에
   기록을 추가하세요:

   | 순서 | 역할 | 산출물 파일 |
   |------|------|-------------|
   | 1 | PM: 요구사항 분석, 기능 목록/우선순위/사용자 시나리오 | `docs/01_기능명세서.md` |
   | 2 | 아키텍트: 기술 스택, 모듈/파일 구조 설계 (가장 단순한 구조 선택) | `docs/02_기술설계서.md` |
   | 3 | 개발자: 설계에 따라 실제 코드 파일들 작성 (실행 방법 포함) | 실제 코드 파일 + `docs/03_구현노트.md` |
   | 4 | QA: 명세서·설계서 대비 구현 검토, 심각도별 이슈 정리 | `docs/04_리뷰보고서.md` |

   **(3) 마무리 커밋** (프로젝트 브랜치에만 커밋됨):

   ```bash
   python main.py finish "<프로젝트 이름>"
   ```

3. 어떤 경우든 **조직(베이스) 브랜치의 파일은 절대 수정하지 마세요.**
   작업이 끝난 뒤 조직 브랜치에서 `git status`가 깨끗해야 합니다.

   주의: 이 격리는 "작업 중 자동/실수 오염"을 막는 것이지, 사람이 나중에
   `project/<이름>` 브랜치를 조직 브랜치로 **의도적으로 PR·머지**하는 것까지
   막지는 않습니다 (머지는 정의상 대상 브랜치를 바꾸는 행위입니다). 조직
   저장소와 물리적으로 완전히 분리하고 싶다면(=머지할 대상 자체가 없게
   하고 싶다면) `python main.py export "<이름>" --new-repo "<저장소이름>"`
   로 별도 GitHub 저장소로 내보내세요.

4. 단순 버그 수정, 리팩터링, 기존 파일 수정 등 "새 프로젝트"가 아닌 일반
   작업은 이 격리 규칙 없이 평소처럼 진행해도 됩니다. 이 규칙은 **완전히
   새로운 앱/프로젝트를 시작할 때만** 적용됩니다.

## 커밋/PR 규칙

- 프로젝트 브랜치(`project/<이름>`)의 커밋 메시지는 한국어로, `feat:`, `fix:`
  같은 접두사를 사용하세요.
- 격리된 프로젝트가 마음에 들면 그때 사람이 직접 머지/PR 결정을 내립니다.
  에이전트가 임의로 조직 브랜치에 머지하지 마세요.

## 보안·안전 규칙 (모든 에이전트 공통, 반드시 준수)

1. **비밀 값 보호**: `.env` 파일의 내용(API 키, 토큰 등)을 채팅 응답,
   커밋, 로그, 산출물 문서 어디에도 출력하거나 복사하지 마세요.
   설정 여부 확인이 필요하면 "키가 설정되어 있는지"만 확인하고 값은
   절대 노출하지 마세요. 새로 만드는 프로젝트에도 같은 원칙을 적용해
   비밀 값은 `.env` + `.gitignore` 조합으로만 관리하세요.
2. **파괴적 git 명령 금지**: 조직(베이스) 브랜치에 대한 `push --force`,
   `reset --hard`, `branch -D`, 이력 재작성(rebase/amend 후 강제 푸시)을
   하지 마세요. 프로젝트 브랜치라도 사용자가 명시적으로 요청한 경우에만
   삭제하세요.
3. **머지 금지**: 어떤 브랜치도 에이전트가 스스로 조직 브랜치(main)에
   머지하지 마세요. PR 생성까지는 가능하지만 머지 버튼은 항상 사람이
   누릅니다.
4. **작업 범위 준수**: 새 프로젝트 작업 중에는 격리 폴더
   (`.projects/<이름>/`) 밖의 파일을 수정하지 마세요. 조직 도구
   (`app_dev_org/` 등) 개선은 사용자가 명시적으로 요청했을 때만,
   조직 브랜치에서 진행하세요.
5. **외부 코드 주의**: 프로젝트 구현 중 외부 패키지를 추가할 때는
   공식 저장소(PyPI/npm 등)의 잘 알려진 패키지만 사용하고, 설치
   스크립트를 통째로 내려받아 실행(`curl | bash` 등)하지 마세요.

## Codex 데스크톱 앱에서 쓸 때 참고 (중요)

Codex 데스크톱 앱은 자체적으로 **Worktree**라는 격리 기능을 가지고 있습니다
(이 저장소의 `branch_manager.py`가 만드는 `project/<이름>` worktree와는
별개의, Codex 자체 기능입니다). 둘을 헷갈리면 안 됩니다.

- **Codex에서 "Local" 모드로 조직 브랜치를 직접 열어 작업하는 경우**:
  위의 "새 앱/프로젝트 개발을 요청받았을 때" 규칙(브랜치 격리)을 **반드시
  그대로 따라야** 조직 브랜치가 오염되지 않습니다.
- **Codex에서 "Worktree" 모드로 새 스레드를 시작하는 경우**: Codex가 이미
  `$CODEX_HOME/worktrees` 아래에 별도 체크아웃을 만들어 격리해주므로,
  그 안에서는 `app_dev_org/main.py new`를 또 실행해 **이중으로 격리할
  필요는 없습니다.** 다만 결과물을 실제 브랜치로 남기고 싶다면 Worktree
  안에서도 이 문서의 "3. 조직(베이스) 브랜치의 파일은 절대 수정하지
  마세요" 원칙은 동일하게 지키세요.
- Codex의 Worktree는 `.gitignore`된 파일(`.env`, `.venv` 등)을 자동으로
  복사하지 않습니다. 이 저장소는 `.env` 파일만 `.worktreeinclude`에
  등록해 자동 복사되게 해두었으니, 파이썬 패키지(`crewai`, `flask` 등)는
  Worktree 안에서 최초 1회 `pip install -r <폴더>/requirements.txt`를
  실행해 직접 설치해야 합니다.
